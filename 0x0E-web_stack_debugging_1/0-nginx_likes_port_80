#/usr/bin/env bash
#Configures script that fix an error that fail to connect to 0 port 80.
sudo apt install nginx -y

sed -i "49s/: /;/"  /etc/nginx/sites-enabled/default

#Then restart nginx.
sudo service nginx restart
